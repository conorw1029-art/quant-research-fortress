We have completed the live-readiness audit, allowlist, kill switch, and dry-run validation harness.

Current classification:
- DRY-RUN READY
- NOT DEMO AUTO-TRADE READY
- NOT LIVE READY

The next blocker is Gate 6:
Broker-native bracket/OCO/OSO order support in tick_tradovate_client.py.

Your next job is to implement bracket-order support safely.

Absolute rules:
1. Do not place any live orders.
2. Do not connect to a live account.
3. Do not ask me to paste real credentials into code.
4. Do not hard-code secrets.
5. Demo mode only.
6. Do not enable --live-auto-trade.
7. Do not run demo auto-trade yet.
8. Do not modify strategy logic.
9. Do not loosen validation rules.
10. Do not remove the bracket-order gate from tick_live_executor.py unless bracket orders are fully implemented and tested in mocked mode.

==================================================
TASK 1 — AUDIT CURRENT TRADOVATE CLIENT
==================================================

Read:

04_codebase/tick_tradovate_client.py

Document exactly what currently exists:
- authentication method
- endpoint selection
- account lookup
- contract lookup
- order placement
- cancel order
- flatten position
- position lookup
- order status lookup
- whether market orders are currently supported
- whether stop orders are supported
- whether limit orders are supported
- whether OCO/OSO/bracket orders are supported

Create:

08_docs/tradovate_client_audit.md

Classify the current client as:
- READ_ONLY
- MARKET_ORDER_ONLY
- BASIC_DEMO_EXECUTION
- BRACKET_CAPABLE_DEMO
- LIVE_READY

Do not call it live-ready.

==================================================
TASK 2 — IMPLEMENT SAFE BRACKET ORDER INTERFACE
==================================================

Patch:

04_codebase/tick_tradovate_client.py

Add a method with a clear interface, for example:

place_bracket_order(
    symbol: str,
    side: str,
    quantity: int,
    entry_type: str,
    entry_price: Optional[float],
    stop_price: float,
    target_price: float,
    account_id: Optional[str] = None,
    demo: bool = True,
    client_order_id: Optional[str] = None,
    dry_run: bool = True
)

Requirements:
1. dry_run=True by default.
2. demo=True by default.
3. Must validate:
   - symbol exists
   - side is BUY or SELL
   - quantity > 0
   - quantity <= configured max contracts
   - stop is on correct side of entry
   - target is on correct side of entry
   - stop distance is not zero
   - target distance is not zero
   - estimated dollar risk <= max_trade_risk_usd
4. Must generate a unique client_order_id.
5. Must log the intended order before sending.
6. Must return a structured result:
   {
     "ok": true/false,
     "mode": "DRY_RUN"|"DEMO",
     "entry_order_id": null or id,
     "stop_order_id": null or id,
     "target_order_id": null or id,
     "client_order_id": "",
     "reason": ""
   }
7. If dry_run=True, it must not call the API.
8. If demo=False, it must refuse unless FORTRESS_LIVE_ENABLE=YES_I_UNDERSTAND, but do not test live mode.

==================================================
TASK 3 — BROKER-NATIVE PROTECTION REQUIREMENT
==================================================

The bracket order must ensure stop and target are broker/platform-native, not merely tracked inside Python.

If Tradovate supports OSO/OCO bracket submission in the current API wrapper, implement it.

If the existing client cannot safely create true native bracket orders yet, do not fake it. Instead:
- implement the interface
- return ok=false with reason="BRACKET_NOT_SUPPORTED_YET"
- keep auto-trading blocked
- document the missing API requirement

Do not pretend bracket orders are implemented if they are not.

==================================================
TASK 4 — ADD MOCK TESTS
==================================================

Create:

04_codebase/test_tradovate_bracket_orders.py

Tests must use mocks only. No real API calls.

Test:
1. dry_run bracket order returns ok=true and no API call is made.
2. invalid side is rejected.
3. invalid quantity is rejected.
4. stop on wrong side is rejected.
5. target on wrong side is rejected.
6. risk above $200 is rejected.
7. demo order refuses if bracket implementation is incomplete.
8. live order refuses without FORTRESS_LIVE_ENABLE.
9. structured result always contains expected keys.
10. tick_live_executor.py still refuses demo-auto-trade if bracket support is not confirmed.

Run the tests with:

C:\Users\conor\Desktop\quant-research\venv_new\Scripts\python.exe test_tradovate_bracket_orders.py

==================================================
TASK 5 — PATCH EXECUTOR INTEGRATION ONLY IF SAFE
==================================================

Patch tick_live_executor.py only enough to:
1. Detect whether tick_tradovate_client.py reports bracket support.
2. Keep demo-auto-trade blocked unless bracket support is confirmed.
3. In dry-run mode, create and log the bracket order payload without sending.
4. In demo mode, use place_bracket_order only if fully supported.
5. Never use naked market entries in demo/live mode.

==================================================
TASK 6 — CREATE REPORT
==================================================

Create:

08_docs/bracket_order_implementation_report.md

It must answer:
1. Is bracket order support fully implemented?
2. Are stops/targets broker-native?
3. Was any real API call made?
4. Did all mock tests pass?
5. Is demo auto-trade now allowed or still blocked?
6. What exact command should I run next?
7. What remains unsafe?

==================================================
STOP CONDITION
==================================================

After this task, stop.

Do not run demo auto-trading.
Do not connect to live.
Do not start any funded account.
Do not implement NinjaTrader bridge yet.